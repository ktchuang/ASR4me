# ── LLM Provider ─────────────────────────────────────────────────────────
# Choose "claude" or "gemini". Default: claude
LLM_PROVIDER=claude

# Required if LLM_PROVIDER=claude
# Get your key from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Required if LLM_PROVIDER=gemini
# Get your key from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# ── ASR Provider ─────────────────────────────────────────────────────────
# Choose "whisper" or "omnilingual". Default: whisper
ASR_PROVIDER=whisper

# ── Whisper settings (used when ASR_PROVIDER=whisper) ────────────────────
# Model size: tiny, base, small, medium, large
WHISPER_MODEL=base
# Language code (ISO 639-1). Default: "zh" (Chinese / 繁體中文)
WHISPER_LANG=zh
# Temperature for decoding. 0 = greedy (most deterministic).
WHISPER_TEMPERATURE=0
# Initial prompt to condition style / vocabulary.
WHISPER_PROMPT=這段音檔包含台灣正體中文和美式英語的混合內容，可能有包含背景音樂的錄音，請只轉錄人聲對話部分，忽略音樂和雜音，可能包含專業術語和客戶資訊。請特別注意數字和專有名詞的準確性。

# ── Omnilingual ASR settings (used when ASR_PROVIDER=omnilingual) ────────
# Model card: omniASR_LLM_300M, omniASR_LLM_1B, omniASR_LLM_7B,
#   omniASR_LLM_Unlimited_300M, omniASR_CTC_300M, etc.
OMNILINGUAL_MODEL=omniASR_LLM_300M
# Language code (ISO 639-3 + script), or leave EMPTY for auto-detect.
# Auto-detect is recommended for multilingual audio (e.g. mixed 華語/台語/English).
# Examples: cmn_Hant (Traditional Chinese), eng_Latn (English), jpn_Jpan (Japanese)
# Note: The API accepts ONE language per audio file. For multilingual content,
#   leave empty to let the model auto-detect the spoken language(s).
OMNILINGUAL_LANG=

# ── Flask ────────────────────────────────────────────────────────────────
# Secret key for session cookies. Generate one for production:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-change-me
