# ── LLM Provider ─────────────────────────────────────────────────────────
# Choose "claude" or "gemini". Default: claude
LLM_PROVIDER=claude

# Required if LLM_PROVIDER=claude
# Get your key from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Required if LLM_PROVIDER=gemini
# Get your key from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# ── Whisper ──────────────────────────────────────────────────────────────
# Model size: tiny, base, small, medium, large
WHISPER_MODEL=base

# Language code. Default: "zh" (Chinese / 繁體中文)
# Set to "en" for English, "ja" for Japanese, etc.
WHISPER_LANG=zh

# Temperature for Whisper decoding. 0 = greedy (most deterministic).
# Higher values (e.g. 0.2–0.8) increase randomness / creativity.
WHISPER_TEMPERATURE=0

# Initial prompt to condition Whisper's style / vocabulary.
# Useful for guiding punctuation, terminology, or domain-specific words.
# Example: "This is a meeting about machine learning and neural networks."
WHISPER_PROMPT=這段音檔包含台灣正體中文和美式英語的混合內容，可能有包含背景音樂的錄音，請只轉錄人聲對話部分，忽略音樂和雜音，可能包含專業術語和客戶資訊。請特別注意數字和專有名詞的準確性。

# ── Flask ────────────────────────────────────────────────────────────────
# Secret key for session cookies. Generate one for production:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-change-me
